#include <stdio.h>
#include <stdlib.h>
/*
Write a program to delete task IDs from the beginning of the linked list. After all deletions, print the entire linked list to show 
the updated list of task IDs. If the list is empty, Print the message as “List is empty”.
*/
// Node structure
struct node{
    int data;
    struct node *next;
};

// Insert at End
void insert_at_end(struct node **head, int data){

    struct node *temp = (struct node*) malloc(sizeof(struct node));
    temp->data = data;
    temp->next = NULL;

    if(*head == NULL){
        *head = temp;
        return;
    }

    struct node *ptr = *head;
    while(ptr->next != NULL){
        ptr = ptr->next;
    }

    ptr->next = temp;
}

// Delete from Beginning
void delete_at_beginning(struct node **head){

    // Check if list empty
    if(*head == NULL){
        return;
    }

    struct node *temp = *head;     // store first node
    *head = (*head)->next;         // move head
    free(temp);                    // free memory
}

// Print List
void print_list(struct node *head){

    if(head == NULL){
        printf("List is empty");
        return;
    }

    struct node *ptr = head;

    while(ptr != NULL){
        printf("%d ", ptr->data);
        ptr = ptr->next;
    }
}

int main(){

    int N, M, id;
    struct node *head = NULL;

    // Input N
    scanf("%d", &N);

    // Build list
    for(int i = 0; i < N; i++){
        scanf("%d", &id);
        insert_at_end(&head, id);
    }
    // Input number of deletions
    scanf("%d", &M);

    // Perform deletions
    for(int i = 0; i < M; i++){
        delete_at_beginning(&head);
    }
    // Print final list
    print_list(head);

    return 0;
}
